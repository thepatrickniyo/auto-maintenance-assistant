# Core dependencies for dataset collection and preparation
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# For LLM fine-tuning (install when ready to train)
# transformers>=4.35.0
# peft>=0.6.0
# accelerate>=0.24.0
# datasets>=2.14.0
# torch>=2.0.0
# bitsandbytes>=0.41.0  # For quantization (optional)

# For deployment (install when ready to deploy)
# gradio>=4.0.0
# streamlit>=1.28.0  # Alternative to Gradio
