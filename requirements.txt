# Core dependencies for dataset collection and preparation
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# For LLM fine-tuning (Colab or local GPU)
transformers>=4.35.0
peft>=0.6.0
accelerate>=0.24.0
datasets>=2.14.0
torch>=2.0.0
bitsandbytes>=0.41.0
trl>=0.7.0

# For evaluation (BLEU, ROUGE)
nltk>=3.8.0
rouge-score>=0.1.2

# For deployment (Gradio chatbot)
gradio>=4.0.0
